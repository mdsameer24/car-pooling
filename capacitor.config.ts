import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.antigravity.carpoolmatch',
  appName: 'CarpoolMatch',
  webDir: 'dist'
};

export default config;
